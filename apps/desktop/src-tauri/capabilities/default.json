{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Compiler UI with dialog access only",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
